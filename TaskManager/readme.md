To Run:

Install TypeScript: npm install -g typescript

Create tsconfig.json:

json
Copy
{
  "compilerOptions": {
    "target": "ES6",
    "outDir": "./dist"
  }
}
Compile: tsc

Serve index.html with a local server
